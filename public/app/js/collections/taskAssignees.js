
//var TaskAssignee = Backbone.Model.extend();

var TaskAssignees = Backbone.Collection.extend({

    //model: TaskAssignee,

    //url: 'task/30001/assignmentpool',

    /*
    parse: function(resp, xhr) {
        return resp.apgroup[0].member;
    }
    */
});
